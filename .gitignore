# Video working directories — masters and camera originals.
# These are multi-GB and far over GitHub's 100 MB per-file limit.
static/videos/original/
static/videos/pre_processed/

# Intermediate/master formats — never commit these, only the web .mp4 outputs.
*.mov
*.mxf
*.webm

# OS / editor cruft
.DS_Store
Thumbs.db
*~
